(defun differentiate (equation function variable)
  "Accepts a tree and differentiates with respect to the given symbol"
  ())

(defun integrate (funct var &optional start end)
  ;;Symbolic Integration
  funct
  var
  ;;Definite Evaluation
  (if (or start end)
      (print t)))
